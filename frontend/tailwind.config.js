/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: '#1D4ED8', // Blue-600
        secondary: '#14B8A6', // Teal-500
        accent: '#0F172A', // Slate-900 background
        neutral: '#F8FAFC', // Slate-50
      },
      backgroundImage: {
        'gradient-blue-teal': 'linear-gradient(135deg, #1D4ED8 0%, #14B8A6 100%)',
        'gradient-teal-blue': 'linear-gradient(135deg, #14B8A6 0%, #1D4ED8 100%)',
      },
      animation: {
        'float': 'float 6s ease-in-out infinite',
        'pulse-slow': 'pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      },
      keyframes: {
        float: {
          '0%, 100%': { transform: 'translateY(0px)' },
          '50%': { transform: 'translateY(-20px)' },
        }
      },
      boxShadow: {
        'glass': '0 8px 32px 0 rgba(31, 38, 135, 0.37)',
        'card': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
      }
    },
  },
  plugins: [],
}
